> 所謂的層疊即疊加的意思，表示樣式可以一層一層的層疊覆蓋。

**層疊性原則 : 樣式衝突，遵循的原則是就近原則，就近原則就是誰離標籤最近，就執行該樣式。**

- 相同選擇器設置相同的樣式，此時一個樣式就會覆蓋另一個衝突的樣式，層疊性主要解決樣式衝突的問題。

```css
div {
	color: red;
	font-size: 12px;
}

div {
	color: pink;
}
```

```html
<div>长江后浪推前浪,前浪死在沙滩上</div>
```