# 150-CSS定位

## 章節資訊

- origin：`origin/150-CSS定位/`
- 原始筆記：11 篇
- 原始資產：3 個
- 狀態總覽：`meta/chapter-status.md`

## 流程紀錄

### 2026-06-29

- 使用者確認 `origin/150-CSS定位` 原始資料已人工整理完成。
- 已同步 `meta/chapter-status.md`：`origin 整理` 標記為 `已完成`。
- 後續流程尚未執行，包含資產命名、alt 與連結文字整理、atomic 切分提案與下游產出。

### 2026-06-30

- 使用 `asset-standardization` 以 apply 模式處理本地資產標準化命名。
- 已將 `origin/150-CSS定位/assets/images/` 內 3 個圖片重新命名為各 Markdown slug 對應的 `*-img-###-hash6.png` 格式，`hash6` 由實體檔案 bytes 的 SHA-256 前 6 碼計算。
- 已同步更新 `origin/150-CSS定位/*.md` 中 3 個可唯一匹配的圖片引用為 `./assets/images/...` 標準路徑。
- `06-固定定位fixed.md` 中 `./images/logo.png` 沒有對應實體資產，判定為 HTML 教學範例路徑並保留不修改。
- 未發現可處理資產的重名衝突、跨文件共用資產或需要人工確認項目。
- 已同步 `meta/chapter-status.md`：`資產命名` 標記為 `已完成`，下一步更新為 `alt/連結文字檢查`。

### 2026-07-01

- 使用 `asset-alt-and-link-text` 以 apply 模式處理本地資產 alt 與連結文字。
- 已掃描 `origin/150-CSS定位/*.md` 與 `origin/150-CSS定位/assets/`，共整理 3 個 Markdown 圖片 alt。
- 已將 `.png` 檔名式 alt 改為可描述圖片內容與 CSS 教學用途的文字。
- 掃描範圍內未發現需要整理的本地附件連結或 HTML assets 圖片 alt。
- `06-固定定位fixed.md` 中 1 處 `./images/logo.png` 為無對應實體資產的教學範例路徑，依規則保留不修改。
- 未發現需要人工確認項目。
- 已同步 `meta/chapter-status.md`：`alt 與連結文字` 標記為 `已完成`，完成率更新為 `21%`，下一步更新為 `atomic 切分提案`。

- 已完成 atomic 切分提案與 atomic notes 產生。
- 已寫入 `atomic/150-CSS定位/`，共 14 篇 atomic notes。
- 已同步 `meta/chapter-status.md`：`atomic 切分提案` 與 `atomic 產生` 標記為 `已完成`，完成率更新為 `36%`，下一步更新為 `atomic 內容審查`。

### 2026-07-01（Atomic 內容審查）

- 使用者已完成 `atomic/150-CSS定位/` Atomic 內容審查，審查範圍共 14 篇 atomic notes。
- 本次同步只更新 `meta/chapter-status.md` 與章節 log，未修改 `atomic/` 或 `origin/` 內容。
- 已同步 `meta/chapter-status.md`：`atomic 內容審查` 標記為 `已完成`，完成率更新為 `43%`，下一步更新為 `notes 生成`。

### 2026-07-02（Notes 生成）

- 已確認 `notes/150-CSS定位/` 已有正式教學筆記，共 8 篇 notes。
- 本次依已完成的 atomic 轉 notes 結果進行狀態同步，只更新 `meta/chapter-status.md` 與章節 log，未修改 `atomic/`、`origin/` 或 `notes/` 內容。
- 已同步 `meta/chapter-status.md`：`notes 生成` 標記為 `已完成`，完成率更新為 `50%`，下一步更新為 `notes 完成檢查`。

### 2026-07-02（Notes 完成檢查）

- 使用者已完成 `notes/150-CSS定位/` 正式教學筆記內容審查，審查範圍共 8 篇 notes。
- 本次同步只更新 `meta/chapter-status.md` 與章節 log，未修改 `origin/`、`atomic/` 或 `notes/` 內容。
- 已同步 `meta/chapter-status.md`：`notes 完成檢查` 標記為 `已完成`，完成率更新為 `57%`，下一步更新為 `notes 索引元資料`。

### 2026-07-02（Notes 索引元資料）

- 已完成 `notes/150-CSS定位/` 索引元資料標記，標記範圍共 8 篇 notes。
- 本次同步只更新 `meta/chapter-status.md` 與章節 log，未修改 `origin/`、`atomic/` 或 `notes/` 內容。
- 已同步 `meta/chapter-status.md`：`notes 索引元資料` 標記為 `已完成`，完成率更新為 `64%`，下一步更新為 `demos 生成`。

### 2026-07-02（Demos 生成）

- 已完成 `notes/150-CSS定位/` 對應教學 demos 生成，產出範圍共 8 組 demos。
- 已寫入 `demos/150-CSS定位/01-定位基礎與-position-組成/`，展示 `position` 與方位屬性的基本組成。
- 已寫入 `demos/150-CSS定位/02-relative-相對定位/`，展示相對定位保留原位置並偏移自身的效果。
- 已寫入 `demos/150-CSS定位/03-absolute-絕對定位/`，展示絕對定位脫離標準流與定位參照。
- 已寫入 `demos/150-CSS定位/04-absolute-居中與子絕父相/`，展示子絕父相與絕對定位居中技巧。
- 已寫入 `demos/150-CSS定位/05-fixed-固定定位/`，展示固定定位相對視口停留的效果。
- 已寫入 `demos/150-CSS定位/06-sticky-黏性定位/`，展示黏性定位在滾動門檻前後的表現。
- 已寫入 `demos/150-CSS定位/07-定位元素的特殊特性與覆蓋行為/`，展示定位元素在尺寸、層疊與覆蓋上的特殊表現。
- 已寫入 `demos/150-CSS定位/08-z-index-定位疊放次序/`，展示 `z-index` 對定位元素疊放順序的影響。
- 已同步 `meta/chapter-status.md`：`demos 生成` 標記為 `已完成`，完成率更新為 `71%`，下一步更新為 `practice 生成`。

## 下一步
- 進入 practice 生成。
