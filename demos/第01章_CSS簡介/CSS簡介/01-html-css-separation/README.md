# HTML 與 CSS 分工

來源筆記：`notes/第01章_CSS簡介/CSS簡介.md`

對應知識點：HTML 負責內容結構，CSS 負責外觀與版面表現。

開啟方式：直接用瀏覽器開啟 `index.html`。

觀察重點：

- 左右兩側的 HTML 內容相同。
- 右側的標題因為 `.styled-example h2` 規則變成橘紅色並放大。
- CSS 改變的是畫面呈現，不會改變 HTML 標籤本身的語意。

常見誤解：把「看起來像標題」誤認為「語意上就是標題」。語意仍由 HTML 結構決定，CSS 只負責外觀。
