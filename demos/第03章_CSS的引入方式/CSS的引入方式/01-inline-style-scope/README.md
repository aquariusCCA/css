# Demo 01：行內樣式作用範圍

來源筆記：`notes/第03章_CSS的引入方式/CSS的引入方式.md`

對應知識點：行內樣式表把 CSS 寫在元素自己的 `style` attribute 中，作用範圍是當前標籤。

開啟方式：直接用瀏覽器開啟 `index.html`。

操作方式：不需要操作，觀察左右兩組段落即可。

觀察重點：右側只有第二段文字變成粉紅色並放大，因為只有它的開始標籤寫了 `style="..."`。

常見誤解：行內樣式很直接，但不適合大量使用；多個元素需要同樣樣式時，逐一寫 `style` 會造成重複與維護困難。
