# 通配符選擇器 Demo

來源筆記：`notes/第06章_選擇器/01-基礎選擇器.md`

對應知識點：通配符選擇器 `*` 會選到頁面中的所有元素。

開啟方式：直接用瀏覽器開啟 `index.html`。

操作方式：不需要操作，觀察整頁文字顏色。

觀察重點：

- 標題、段落、清單、按鈕與程式碼文字都變成紅色。
- 這不是局部樣式，而是 `*` 選到所有元素造成的結果。
- 若只想修改局部內容，應改用標籤、class 或 id 等更精準的 selector。

常見誤解：把 `*` 當成方便的萬用解法。它適合做全頁基礎規則或 reset，但不適合取代精準選擇。
