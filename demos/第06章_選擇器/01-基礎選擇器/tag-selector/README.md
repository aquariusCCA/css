# 標籤選擇器 Demo

來源筆記：`notes/第06章_選擇器/01-基礎選擇器.md`

對應知識點：標籤選擇器 `p` 會選到頁面上所有 `<p>` 元素。

開啟方式：直接用瀏覽器開啟 `index.html`。

操作方式：不需要操作，左右比較修改前與修改後。

觀察重點：

- 修改後兩個 `<p>` 都變成綠色。
- `<div>` 沒有被 `p` 選到，所以保持原樣。
- 標籤選擇器不是只選第一個符合元素，而是選所有同名標籤。

常見誤解：以為 `p` 只會影響第一段；實際上所有 `<p>` 都會被同一條規則選中。
