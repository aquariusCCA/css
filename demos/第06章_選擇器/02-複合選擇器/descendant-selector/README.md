# 後代選擇器 Demo

來源筆記：`notes/第06章_選擇器/02-複合選擇器.md`

對應知識點：後代選擇器 `.nav a` 會選到 `.nav` 裡所有層級的 `<a>`。

開啟方式：直接用瀏覽器開啟 `index.html`。

觀察重點：

- 修改後只有導覽列裡的連結變成橘色。
- 導覽列外的「閱讀更多文章」沒有被選到。
- 空格表示後代關係，不代表直接子層。

常見誤解：以為 `.nav a` 只會選到直接子元素；實際上只要在 `.nav` 裡面，不管隔幾層都可能被選到。
