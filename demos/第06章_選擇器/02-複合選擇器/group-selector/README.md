# 並集選擇器 Demo

來源筆記：`notes/第06章_選擇器/02-複合選擇器.md`

對應知識點：並集選擇器使用逗號 `,`，讓多個 selector 共用同一組樣式。

開啟方式：直接用瀏覽器開啟 `index.html`。

觀察重點：

- 左側 `h2 p` 沒有逗號，表示後代關係，不是集體選取。
- 右側 `h2, p, .note li` 會同時選到標題、段落與 `.note` 裡的清單項目。
- 逗號改變 selector 的整體意義。

常見誤解：少打一個逗號，`h2, p` 會變成 `h2 p`，選取結果完全不同。
