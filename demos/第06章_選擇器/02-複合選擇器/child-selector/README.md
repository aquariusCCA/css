# 子選擇器 Demo

來源筆記：`notes/第06章_選擇器/02-複合選擇器.md`

對應知識點：子選擇器 `.menu > a` 只選直接子元素。

開啟方式：直接用瀏覽器開啟 `index.html`。

觀察重點：

- 左側 `.menu a` 會選到 `.menu` 裡所有層級的連結。
- 右側 `.menu > a` 只選到直接放在 `.menu` 裡的「主選單」。
- 包在 `<p>` 裡的「子選單」不是 `.menu` 的直接子元素。

常見誤解：把 `.menu a` 和 `.menu > a` 當成一樣；前者看後代，後者看直接父子關係。
