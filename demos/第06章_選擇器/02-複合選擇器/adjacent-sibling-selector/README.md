# 相鄰兄弟選擇器 Demo

來源筆記：`notes/第06章_選擇器/02-複合選擇器.md`

對應知識點：相鄰兄弟選擇器 `h2 + p` 只選緊接在 `h2` 後面的第一個 `p`。

開啟方式：直接用瀏覽器開啟 `index.html`。

觀察重點：

- 修改後只有標題後面的第一段被選到。
- 標題前面的段落不會被影響。
- 第二段不是緊接在標題後面的第一個兄弟，所以不會被 `+` 選到。

常見誤解：`+` 只能往後選緊接的下一個兄弟，不會往前選，也不會選後面所有兄弟。
