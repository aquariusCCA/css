# 通用兄弟選擇器 Demo

來源筆記：`notes/第06章_選擇器/02-複合選擇器.md`

對應知識點：通用兄弟選擇器 `h2 ~ p` 會選到指定元素後方所有同層的符合元素。

開啟方式：直接用瀏覽器開啟 `index.html`。

觀察重點：

- 修改後標題後方兩個段落都被選到。
- 標題前面的段落不會被選到。
- 被選到的元素必須和左邊的元素在同一層。

常見誤解：`~` 不會往前選，也不會跨到不同父元素裡選。
