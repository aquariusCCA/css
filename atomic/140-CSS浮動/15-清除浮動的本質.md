---
source:
  - 'origin/140-CSS浮動/09-清除浮動.md / # 清除浮動的本質'
---

# 清除浮動的本質

清除浮動的本質，是處理浮動元素造成的父級高度塌陷與後續布局影響。

常見做法是加入帶有 `clear: both` 的清除元素或偽元素，或讓父級建立 BFC，讓父級能包住浮動子元素造成的高度影響。父級有了高度，就不會影響下面的標準流。

如果父盒子本身有高度，則不需要清除浮動。

清除浮動的策略是閉合浮動：

只讓浮動在父盒子內部影響，不影響父盒子外面的其他盒子。
