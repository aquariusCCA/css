---
source:
  - 'origin/140-CSS浮動/09-清除浮動.md / ### 父級添加雙偽元素'
---

# 雙偽元素清除浮動

雙偽元素清除浮動也是添加給父元素。它同時使用 `::before` 和 `::after`，常用於建立 clearfix。

優點：代碼更簡潔。

缺點：寫法比單個 `::after` 更固定；如果需要兼容很舊的瀏覽器，可能會看到舊式單冒號或 `*zoom: 1` 寫法。

```css
.clearfix::before,
.clearfix::after {
  content: "";
  display: table;
}

.clearfix::after {
  clear: both;
}

.clearfix {
  *zoom: 1;
}

.box {
  width: 800px;
  border: 1px solid blue;
  margin: 0 auto;
}

.damao {
  float: left;
  width: 300px;
  height: 200px;
  background-color: purple;
}

.ermao {
  float: left;
  width: 200px;
  height: 200px;
  background-color: pink;
}

.footer {
  height: 200px;
  background-color: black;
}
```

```html
<body>
  <div class="box clearfix">
    <div class="damao">大毛</div>
    <div class="ermao">二毛</div>
  </div>

  <div class="footer"></div>
</body>
```
