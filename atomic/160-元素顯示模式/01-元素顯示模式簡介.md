---
source:
  - 'origin/160-元素顯示模式/01-簡介.md / 全文'
---

# 元素顯示模式簡介

網頁的標籤非常多，不同地方會用到不同類型的標籤。了解標籤各自的顯示特點，可以更好地布局頁面。

元素顯示模式，就是元素以什麼方式進行顯示。

例如：

- `<div>` 會自己占一行。
- 一行可以放多個 `<span>`。

HTML 元素一般分為兩種類型：

- 塊元素。
- 行內元素。
