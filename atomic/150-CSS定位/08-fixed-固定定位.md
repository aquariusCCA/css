---
source:
  - 'origin/150-CSS定位/06-固定定位fixed.md / 開頭至範例程式碼'
---

# fixed 固定定位

固定定位會讓元素固定於瀏覽器可視區的位置。

主要使用場景：瀏覽器頁面滾動時，元素的位置不會改變。

固定定位的參考點：

- 以瀏覽器的可視窗口為參照點移動元素。
- 參考它的視口。
- 對 PC 瀏覽器來說，視口就是觀看網頁的那扇「窗戶」。

固定定位的特點：

- 一般情況下與父元素位置無關，會相對瀏覽器視口定位；但某些會建立包含塊的祖先屬性，例如 `transform`，可能影響固定定位的參照。
- 不隨滾動條滾動。
- 不再佔有原先的位置，也就是脫標。
- 脫離文檔流會對後面的兄弟元素、父元素有影響。
- `left` 不建議和 `right` 一起設定，`top` 不建議和 `bottom` 一起設定。
- 固定定位和浮動不能同時設定；如果同時設定，浮動失效，以定位為主。
- 固定定位元素也能透過 margin 調整位置，但不推薦這樣做。
- 無論原本是行內、行內塊或塊級元素，設定為固定定位後，都會變成定位元素。
- 定位元素預設寬高由內容撐開，也能自由設定寬高。
- 固定定位也是脫標的，也可以看作一種特殊的絕對定位。

```css
選擇器 {
  position: fixed;
}
```

範例：

```css
.dj {
  position: fixed;
  top: 100px;
  left: 40px;
}
```

```html
<body>
  <div class="dj">
    <img src="./images/logo.png" alt="">
  </div>
  <p>请尽情吩咐妲己，主人</p>
  <p>请尽情吩咐妲己，主人</p>
  <p>请尽情吩咐妲己，主人</p>
</body>
```

> 範例資產說明：原始範例在 fenced code block 內使用 `./images/logo.png`；此為示意碼，非本章可追溯資產引用。依 atomic 切分規則，fenced code block 內的示例路徑保留原樣，不改成 assets 路徑。
> 待後續內容審查確認：「left 不能和 right 一起設置，top 和 bottom 不能一起設置」屬於入門簡化說法，正式 notes 可補充同時設定時的實際約束規則。
