---
source:
  - 'origin/050-瀏覽器的私有前綴/01-瀏覽器的私有前綴.md / 開頭提示'
  - 'origin/050-瀏覽器的私有前綴/01-瀏覽器的私有前綴.md / # 什麼時候去掉私有前綴'
---

# 何時去掉私有前綴

當一個屬性成為標準，並且被 Firefox、Chrome 等主流瀏覽器的最新版普遍兼容時，就可以去掉該屬性的瀏覽器前綴。

實際編碼時，不必刻意記憶所有瀏覽器私有前綴，也不需要每個前綴都手動查詢。

常用的 CSS3 新特性，主流瀏覽器通常都已支持。即使為了老瀏覽器需要添加前綴，也可以借助現代構建工具自動添加私有前綴。
