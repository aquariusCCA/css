---
source:
  - 'origin/050-瀏覽器的私有前綴/01-瀏覽器的私有前綴.md / # 書寫順序'
---

# 私有前綴的書寫順序

書寫帶有私有前綴的 CSS3 屬性時，建議先寫私有的 CSS3 屬性，再寫標準的 CSS3 屬性。

提倡寫法如下：

```css
-moz-border-radius: 10px;

-webkit-border-radius: 10px;

-o-border-radius: 10px;

border-radius: 10px;
```

這種順序會讓支援標準屬性的瀏覽器優先採用最後的標準寫法。
