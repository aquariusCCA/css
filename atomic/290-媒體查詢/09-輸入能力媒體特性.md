---
source:
  - 'origin/290-媒體查詢/03-媒體特性.md / ## 判斷是否能懸停 any-hover、hover'
  - 'origin/290-媒體查詢/03-媒體特性.md / ## 判斷是否可點擊 any-pointer、pointer'
---

# 輸入能力媒體特性

輸入能力媒體特性可以根據設備是否支援懸停、指針輸入，以及指針精確度來調整互動樣式。

## hover 與 any-hover

`hover` 用來檢測設備是否支持懸停操作。

- `hover: none`：表示設備不支持懸停操作，例如觸摸屏設備。
- `hover: hover`：表示設備支持懸停操作，例如桌面設備上的鼠標。

```css
/* 當設備支持懸停操作時應用 */
@media (hover: hover) {
  .button {
    background-color: blue;
  }

  .button:hover {
    background-color: darkblue;
  }
}

/* 當設備不支持懸停操作時應用 */
@media (hover: none) {
  .button {
    background-color: gray;
  }
}
```

`any-hover` 檢測設備的任何一個輸入設備是否支持懸停操作。

- `any-hover: none`：表示所有輸入設備都不支持懸停操作。
- `any-hover: hover`：表示至少一個輸入設備支持懸停操作。

```css
/* 當任何輸入設備支持懸停操作時應用 */
@media (any-hover: hover) {
  .button {
    background-color: blue;
  }

  .button:hover {
    background-color: darkblue;
  }
}

/* 當所有輸入設備都不支持懸停操作時應用 */
@media (any-hover: none) {
  .button {
    background-color: gray;
  }
}
```

## pointer 與 any-pointer

`pointer` 檢測設備是否支持指針輸入，並提供不同值表示指針精確度。

- `pointer: none`：表示設備不支持指針輸入。
- `pointer: coarse`：表示設備支持指針輸入，但指針精確度較低，例如觸摸屏。
- `pointer: fine`：表示設備支持指針輸入，且指針精確度較高，例如鼠標或觸控板。

```css
/* 當設備的指針輸入精確度較高時應用 */
@media (pointer: fine) {
  .button {
    padding: 10px 20px;
  }
}

/* 當設備的指針輸入精確度較低時應用 */
@media (pointer: coarse) {
  .button {
    padding: 20px 40px;
  }
}

/* 當設備不支持指針輸入時應用 */
@media (pointer: none) {
  .button {
    padding: 20px;
  }
}
```

`any-pointer` 檢測設備的任何一個輸入設備是否支持指針輸入。

- `any-pointer: none`：所有輸入設備都不支持指針輸入。
- `any-pointer: coarse`：至少一個輸入設備支持指針輸入，但精確度較低。
- `any-pointer: fine`：至少一個輸入設備支持指針輸入，且精確度較高。

```css
/* 當任何一個輸入設備的指針精確度較高時應用 */
@media (any-pointer: fine) {
  .button {
    padding: 10px 20px;
  }
}

/* 當任何一個輸入設備的指針精確度較低時應用 */
@media (any-pointer: coarse) {
  .button {
    padding: 20px 40px;
  }
}

/* 當所有輸入設備都不支持指針輸入時應用 */
@media (any-pointer: none) {
  .button {
    padding: 20px;
  }
}
```

這些媒體特徵可以幫助你根據不同設備提供合適的互動替代方式，例如為觸摸屏提供更大的點擊區域，或為桌面設備提供懸停效果。
