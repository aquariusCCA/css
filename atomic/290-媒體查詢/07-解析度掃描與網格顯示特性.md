---
source:
  - 'origin/290-媒體查詢/03-媒體特性.md / ## 分辨率（resolution）'
  - 'origin/290-媒體查詢/03-媒體特性.md / ## 掃描（scan）'
  - 'origin/290-媒體查詢/03-媒體特性.md / ## 網格（grid）'
---

# 解析度、掃描與網格顯示特性

這組媒體特徵用來判斷顯示設備能力，包括解析度、掃描方式，以及是否為網格顯示。

## resolution

`resolution` 用來檢測設備顯示器的解析度，可根據顯示器的像素密度或分辨率應用不同樣式。

- `dpi`：每英寸顯示的點數。
- `dppx`：每像素的點數。1 dppx 等於 96 dpi。

```css
/* 當設備解析度為 150dpi 或更高時應用 */
@media (min-resolution: 150dpi) {
  body {
    background-image: url('high-res-background.jpg');
  }
}

/* 當設備解析度為 2dppx 或更高時應用 */
@media (min-resolution: 2dppx) {
  body {
    background-image: url('high-res-background.jpg');
  }
}

/* 當設備解析度為 72dpi 或更低時應用 */
@media (max-resolution: 72dpi) {
  body {
    background-image: url('low-res-background.jpg');
  }
}
```

`resolution` 常用於適配高解析度顯示器、選擇合適圖片資源、改善不同解析度設備上的用戶體驗。

## scan

`scan` 用於檢測顯示設備的掃描方式。

- `interlace`：表示設備使用逐場掃描。
- `progressive`：表示設備使用逐行掃描。

```css
/* 當設備使用逐行掃描時應用 */
@media (scan: progressive) {
  body {
    background-color: lightblue;
  }
}

/* 當設備使用逐場掃描時應用 */
@media (scan: interlace) {
  body {
    background-color: lightgreen;
  }
}
```

原始筆記強調：`scan` 媒體特性的使用場景非常有限，實際項目更常使用其他媒體特性，例如 `screen` 和 `print`。

## grid

`grid` 用來檢測設備是否使用網格來顯示內容，這裡的 `grid` 與 CSS Grid Layout 無直接關聯。

- `grid: 1`：表示設備是基於網格顯示的。
- `grid: 0`：表示設備不是基於網格顯示，而是像素顯示。

`grid` 只接受 `0` 或 `1` 這類媒體查詢布林值；`true` 和 `false` 不是合法值。也可以使用 `(grid)` 這種布林上下文寫法來測試是否為網格顯示設備。

```css
/* 當設備基於網格顯示時，應用特定樣式 */
@media (grid: 1) {
  body {
    font-size: 12px;
  }
}

/* 當設備不基於網格顯示時，應用其他樣式 */
@media (grid: 0) {
  body {
    font-size: 16px;
  }
}
```

`grid` 主要適用於特殊或舊式設備，現代網頁開發中的使用率較低。
